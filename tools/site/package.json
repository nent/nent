{
  "name": "@nent/site",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "prebuild": "rm -fR www",
    "build": "hugo -s ./src --ignoreCache --cleanDestinationDir --forceSyncStatic --environment develop --debug",
    "start": "hugo server  -v --templateMetrics --gc --ignoreCache --cleanDestinationDir --forceSyncStatic"
  },
  "devDependencies": {
    "@tailwindcss/forms": "^0.5.2",
    "@tailwindcss/typography": "^0.5.4",
    "autoprefixer": "^10.4.8",
    "flowbite": "^1.5.1",
    "npm-run-all": "^4.1.5",
    "postcss": "^8.4.14",
    "postcss-cli": "^10.0.0",
    "tailwindcss": "^3.1.7"
  }
}
